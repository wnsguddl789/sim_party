export interface Detail {
  cloName: string;
  cloSize: string;
  imagePath: string;
  bookingList: {
    currentBooking_1: string;
    currentBooking_2: string;
    currentBooking_3: string;
    extraBooking: number;
  };
}
